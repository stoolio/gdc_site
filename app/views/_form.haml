- defaults = { method: 'post', data: { abide: true } }.merge(@form_data)
.alert-box.radius{ style: 'display: none;', data: { results_for: @form_data[:id], alert: true } }
  %h3{ style: 'color:white;' }
  %a.close{ href: '#' } &times;
%form{defaults}
  = yield_content :form
  = partial :submit_button
